.mainTitle cf.alert
.content
  .title 簡介
  div
    div 用 popup 模擬 alert 的功能, 避免 browser 禁用 alert
    div 使用 cf_alert.css 編輯樣式
.content
  .title 函式
  div
    .tip2
      | cf.alert(msg, [opt = {}]);
      span.codeLink 程式範例
    .subTitle1 opt 參數
    div
      div $target: alert 視窗要放至的位置, 預設 cf.$body
      div cb: 點選 alert 視窗的 ok 按鈕之後觸發的 cb
    .codeTarget
      div cf.alert('This is alert msg.', {
      .tab1 cb: function() { ... }
      div });